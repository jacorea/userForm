import React from 'react'

const User = ({ userName, age }) => {
    return (
        <div>
            <div>Username: {userName}</div>
            <div>age: {age}</div>
        </div>
    )
}

export default User
